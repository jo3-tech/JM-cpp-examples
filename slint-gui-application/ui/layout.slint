export global Layout {
  out property <length> preferred_window_width: 960px;
  out property <length> preferred_window_height: 540px;  
  out property <length> window_padding: 10px;
  out property <length> panel_padding: 10px;
  out property <length> panel_spacing: 10px;
}