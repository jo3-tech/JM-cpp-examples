export global Layout {
  in property <length> window_padding: 10px;
  in property <length> panel_padding: 10px;
  in property <length> panel_spacing: 10px;
  in property <length> widget_group_padding: 20px;
}