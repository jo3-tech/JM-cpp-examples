export global Theme {
  out property <length> default_font_size: 18px;
  out property <length> panel_border_radius: 10px;
  out property <color> panel_background: #093981;
  out property <length> text-font-size: default_font_size * (3/4);  
  out property <color> text_color: springgreen;
}