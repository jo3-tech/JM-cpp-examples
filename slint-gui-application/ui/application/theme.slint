export global Theme {
  out property <length> default-font-size: 18px;
  out property <length> panel-border-radius: 10px;
  out property <color> panel-background: #093981;
  out property <length> text-font-size: default_font_size * (3/4);  
  out property <color> text-color: springgreen;
}