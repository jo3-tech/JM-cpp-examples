export global Layout {
  out property <length> preferred-window-width: 960px;
  out property <length> preferred-window-height: 540px;  
  out property <length> window-padding: 10px;
  out property <length> panel-padding: 10px;
  out property <length> panel-spacing: 10px;
}